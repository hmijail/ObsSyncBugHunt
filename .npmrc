# refuse to install under a Node/npm outside package.json "engines"
engine-strict=true
# new deps are pinned to an exact version (no floating ^ ranges)
save-exact=true
# suppress npm's own "a new npm version is available" nag — unrelated to dependency security
# (no effect on npm audit or what gets installed), just quieter output for anyone in this repo
update-notifier=false
