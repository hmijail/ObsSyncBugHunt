# README.md is off-limits
Keep track of any recommended edits in a separate file, e.g. README_proposed.md

# Check the docs/ folder 
There's lots of design notes, known traps, etc. For example, what can be trusted or not from obsidian-cli in spite of its documentation.
Keep those notes up-to-date, and prune whatever is outdated or no longer relevant. 
o not record minutiae that might change with time; e.g., if you need to record a design decision as based on a benchmark, then do not record the benchmark results, but create a script that can be re-run at will to demonstrate the timings that justify the design; and maybe wire that script to the check-assumptions Makefile target.
Keep those notes in sync with any comments in the codebase.
