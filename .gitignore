node_modules/
dist/
*.log
.DS_Store
runs/
# Forensic screenshots from `make health` / debugging
_*.png

# Captured Obsidian Sync login (account token + vault link) — never commit
secrets/

# Personal, per-machine Claude Code settings (shared config goes in settings.json)
.claude/settings.local.json
# Personal editor settings
.vscode/
*.code-workspace

# Queued README edits (the README is hand-written; these are notes toward it)
README_pending.md

# check-assumptions' N1AaN2Aa race step keeps its rep logs here when the step fails
check-assumptions-runs/
